#include "DialogInfo.h"

CDialogInfo::CDialogInfo( int id_resource, CWnd* parent, CData* data )
: CDialog( id_resource, pParent )
, pParent( parent )
, pData( data )
{
}

CDialogInfo::~CDialogInfo( void )
{
	pParent = nullptr;
	pData = nullptr;
}
